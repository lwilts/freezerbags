services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - TZ=Europe/London
      - RESTORE_TOKEN=${RESTORE_TOKEN:?RESTORE_TOKEN must be set}
